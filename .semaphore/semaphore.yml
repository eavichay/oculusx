version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Setup"
    task:
      jobs:
        - name: install
          commands:
          -  checkout
          - "npm install"
  - name: "Unit tests"
    task:
      jobs:
      - name: RSpec
        commands:
          - "npm test"